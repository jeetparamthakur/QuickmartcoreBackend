services:
  - type: web
    name: quickmartcore-backend
    runtime: node
    branch: dev
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
